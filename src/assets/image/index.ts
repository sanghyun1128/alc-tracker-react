import defaultCocktail from './default-cocktail.png';
import defaultProfile from './default-profile.png';
import defaultSpirit from './default-spirit.png';
import defaultWine from './default-wine.png';

export const Images = {
  DEFAULT_COCKTAIL: defaultCocktail,
  DEFAULT_PROFILE: defaultProfile,
  DEFAULT_SPIRIT: defaultSpirit,
  DEFAULT_WINE: defaultWine,
};
